spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/projeto_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  #jpa:
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.PostgreSQLDialect

